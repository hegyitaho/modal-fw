import { OpenedModalProps } from '../modal.types';
export declare function ImageModal({ src, altText }: {
    src: string;
    altText: string;
}): ({ onClose, title }: OpenedModalProps) => import("react/jsx-runtime").JSX.Element;
